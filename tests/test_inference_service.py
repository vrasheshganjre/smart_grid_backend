"""
Unit tests for the inference service.

TODO: implement once ModelAdapter.load and ModelAdapter.predict are wired.
"""
import pytest
import pandas as pd

# from app.services.inference_service import run_inference


# TODO: test that run_inference appends a 'prediction' column to the dataframe
# TODO: test that run_inference raises on unexpected feature shape
# TODO: mock ModelAdapter so tests run without a real model artefact on disk
